<!-- markdownlint-disable MD036 MD041 -->
<!-- Rendered as a GitHub issue body, where the issue title supplies the H1.
     The bold-paragraph opening matches the existing cluster-card convention
     (e.g. issue #80). Do not "promote" to a real heading without updating
     the existing cluster cards too. -->

**Slice card: {{SLICE_NUM}} — {{SLICE_TITLE}}**

Tracking issue for the spec at [`{{SPEC_PATH}}`]({{SPEC_PATH}}).

## Scope

{{SUMMARY}}

## Status

- [x] Spec drafted
- [ ] Plan generated (`plan.md`)
- [ ] Tasks broken down (`tasks.md`)
- [ ] Implementation PR opened
- [ ] PR merged + verified

## Linked PR

Will be auto-linked when an implementation PR includes `Closes #<this-issue-number>` in its body (per the convention added in PR #109).

---

_Auto-generated by [`scripts/spec-to-issue/create.sh`](../scripts/spec-to-issue/create.sh) at the time the spec was authored. The trailing `<!-- tracking-issue: #N -->` marker in the spec file is how the script detects existing tracking issues on re-run; do not delete it without also closing the linked issue._
