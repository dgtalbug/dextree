import { describe, it } from "vitest";

describe("MermaidPreviewPanel (slice 029 scaffold)", () => {
  it.todo("renders empty placeholder when preview is null (T009)");
  it.todo("renders source pane and triggers renderSource for ok preview (T009)");
  it.todo("renders fail-closed alert for empty/oversized/unsupported status (T009)");
  it.todo("shows render-error message when renderSource resolves to render-error (T009)");
  it.todo("disables overlapping renders (T009)");
});
