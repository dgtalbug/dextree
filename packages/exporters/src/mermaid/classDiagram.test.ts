import { describe, it } from "vitest";

describe("classDiagram (slice 028 scaffold)", () => {
  it.todo("groupClassDiagramEntries — class/interface/enum detection (T011)");
  it.todo("groupClassDiagramEntries — methods grouped by enclosingSymbolId (T011)");
  it.todo("groupClassDiagramEntries — inheritsFrom / instantiates in-scope filter (T011)");
  it.todo("validateClassDiagramExport — decision table coverage (T012)");
  it.todo("serializeToClassDiagram — integration (T013)");
  it.todo("end-to-end method-grouping with same-named methods (T021)");
  it.todo("no-classes-in-scope refusal (T024)");
  it.todo("missing-UML-detail absence (T025)");
  it.todo("class-with-no-relationships (T026)");
});
