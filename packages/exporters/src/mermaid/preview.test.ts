import { describe, it } from "vitest";

describe("generateMermaidPreview (slice 029 scaffold)", () => {
  it.todo("flowchart route delegates to scoped serializer with resolved theme (T008)");
  it.todo("classDiagram returns unsupported until PR-B routes it (T015)");
  it.todo("sequenceDiagram returns unsupported with slice-031 reason (T015)");
  it.todo("ok result carries source + title derived from options (T008)");
  it.todo("scoped-serializer empty/oversized errors classify into status (T008)");
});
